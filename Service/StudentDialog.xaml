<Window x:Class="Service.StudentDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Ученик" Height="300" Width="480"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize">
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="170"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <TextBlock Grid.Row="0" Grid.Column="0" Text="ФИО:" VerticalAlignment="Center"/>
        <TextBox x:Name="tbFio" Grid.Row="0" Grid.Column="1" Height="26" Margin="0,2"/>

        <TextBlock Grid.Row="1" Grid.Column="0" Text="Дата рождения (YYYY-MM-DD):" VerticalAlignment="Center"/>
        <TextBox x:Name="tbBirth" Grid.Row="1" Grid.Column="1" Height="26" Margin="0,2"/>

        <TextBlock Grid.Row="2" Grid.Column="0" Text="Телефон родителя:" VerticalAlignment="Center"/>
        <TextBox x:Name="tbParent" Grid.Row="2" Grid.Column="1" Height="26" Margin="0,2"/>

        <TextBlock Grid.Row="3" Grid.Column="0" Text="Класс:" VerticalAlignment="Center"/>
        <ComboBox x:Name="cbClass" Grid.Row="3" Grid.Column="1" Height="26" Margin="0,2"
                  DisplayMemberPath="Title" SelectedValuePath="Id"/>

        <StackPanel Grid.Row="5" Grid.ColumnSpan="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
            <Button Content="OK" Width="90" Margin="6,0" Click="Ok_Click"/>
            <Button Content="Отмена" Width="90" Margin="6,0" Click="Cancel_Click"/>
        </StackPanel>
    </Grid>
</Window>
