<Window x:Class="Service.OrderDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Service"
        mc:Ignorable="d"
        Title="Новый заказ" 
        Height="400" 
        Width="500"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <TextBlock Grid.Row="0" Grid.ColumnSpan="2" 
                   Text="Создание нового заказа" 
                   FontSize="14" FontWeight="Bold" Margin="0,0,0,10"/>

        <TextBlock Grid.Row="1" Grid.Column="0" Text="Автомобиль:" 
                   VerticalAlignment="Center" Margin="0,0,10,5"/>
        <ComboBox Grid.Row="1" Grid.Column="1" x:Name="cbCar" Height="30" Margin="0,0,0,5" 
                  DisplayMemberPath="Автомобиль" 
                  SelectedValuePath="Код_автомобиля"
                  SelectedValue="{Binding CarId}"/>

        <TextBlock Grid.Row="2" Grid.Column="0" Text="Менеджер:" 
                   VerticalAlignment="Center" Margin="0,0,10,5"/>
        <ComboBox Grid.Row="2" Grid.Column="1" x:Name="cbManager" Height="30" Margin="0,0,0,5" 
                  DisplayMemberPath="ФИО" 
                  SelectedValuePath="Код_менеджера"
                  SelectedValue="{Binding ManagerId}"/>

        <TextBlock Grid.Row="3" Grid.Column="0" Text="Работник:" 
                   VerticalAlignment="Center" Margin="0,0,10,5"/>
        <ComboBox Grid.Row="3" Grid.Column="1" x:Name="cbWorker" Height="30" Margin="0,0,0,10" 
                  DisplayMemberPath="ФИО" 
                  SelectedValuePath="Код_работника"
                  SelectedValue="{Binding WorkerId}"/>

        <TextBlock Grid.Row="4" Grid.Column="0" Text="Описание проблемы:" 
                   VerticalAlignment="Top" Margin="0,10,10,0"/>
        <TextBox Grid.Row="4" Grid.Column="1" Height="120" Margin="0,10,0,20" 
                 Text="{Binding Problem, UpdateSourceTrigger=PropertyChanged}"
                 AcceptsReturn="True" TextWrapping="Wrap" VerticalScrollBarVisibility="Auto"/>

        <StackPanel Grid.Row="5" Grid.ColumnSpan="2" 
                    Orientation="Horizontal" HorizontalAlignment="Right">
            <Button x:Name="BtnOK" Content="Создать" Width="80" Height="30" Margin="5" 
                    Click="BtnOK_Click"/>
            <Button x:Name="BtnCancel" Content="Отмена" Width="80" Height="30" Margin="5" 
                    Click="BtnCancel_Click"/>
        </StackPanel>
    </Grid>
</Window>
