<Window x:Class="Service.GradeDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Оценка" Height="300" Width="460" WindowStartupLocation="CenterOwner" ResizeMode="NoResize">
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="170"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <TextBlock Grid.Row="0" Grid.Column="0" Text="Ученик:" VerticalAlignment="Center"/>
        <ComboBox x:Name="cbStudent" Grid.Row="0" Grid.Column="1" Height="26" Margin="0,2"
                  DisplayMemberPath="Title" SelectedValuePath="Id"/>

        <TextBlock Grid.Row="1" Grid.Column="0" Text="Предмет:" VerticalAlignment="Center"/>
        <ComboBox x:Name="cbSubject" Grid.Row="1" Grid.Column="1" Height="26" Margin="0,2"
                  DisplayMemberPath="Title" SelectedValuePath="Id"/>

        <TextBlock Grid.Row="2" Grid.Column="0" Text="Дата (YYYY-MM-DD):" VerticalAlignment="Center"/>
        <TextBox x:Name="tbDate" Grid.Row="2" Grid.Column="1" Height="26" Margin="0,2"/>

        <TextBlock Grid.Row="3" Grid.Column="0" Text="Оценка (1..5):" VerticalAlignment="Center"/>
        <TextBox x:Name="tbGrade" Grid.Row="3" Grid.Column="1" Height="26" Margin="0,2"/>

        <TextBlock Grid.Row="4" Grid.Column="0" Text="Тема:" VerticalAlignment="Center"/>
        <TextBox x:Name="tbTopic" Grid.Row="4" Grid.Column="1" Height="26" Margin="0,2"/>

        <StackPanel Grid.Row="6" Grid.ColumnSpan="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
            <Button Content="OK" Width="90" Margin="6,0" Click="Ok_Click"/>
            <Button Content="Отмена" Width="90" Margin="6,0" Click="Cancel_Click"/>
        </StackPanel>
    </Grid>
</Window>
