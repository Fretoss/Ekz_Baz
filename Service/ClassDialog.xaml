<Window x:Class="Service.ClassDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Класс" Height="240" Width="460"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize">
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="160"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <TextBlock Grid.Row="0" Grid.Column="0" Text="Название (например 7А):" VerticalAlignment="Center"/>
        <TextBox x:Name="tbName" Grid.Row="0" Grid.Column="1" Height="26" Margin="0,2"/>

        <TextBlock Grid.Row="1" Grid.Column="0" Text="Год обучения:" VerticalAlignment="Center"/>
        <TextBox x:Name="tbYear" Grid.Row="1" Grid.Column="1" Height="26" Margin="0,2"/>

        <TextBlock Grid.Row="2" Grid.Column="0" Text="Классный руководитель:" VerticalAlignment="Center"/>
        <ComboBox x:Name="cbHead" Grid.Row="2" Grid.Column="1" Height="26" Margin="0,2"
                  DisplayMemberPath="Title" SelectedValuePath="Id"/>

        <StackPanel Grid.Row="4" Grid.ColumnSpan="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
            <Button Content="OK" Width="90" Margin="6,0" Click="Ok_Click"/>
            <Button Content="Отмена" Width="90" Margin="6,0" Click="Cancel_Click"/>
        </StackPanel>
    </Grid>
</Window>
